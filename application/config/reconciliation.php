<?php
defined('BASEPATH') OR exit('No direct script access allowed');

/*
| 결제 대사(cli/verify payments)에서 뺄 결제.
|
| **실험으로 일부러 어긋나게 만든 결제만** 적는다. 원인을 모르는 어긋남을
| 여기 넣어 조용하게 만들면 대사가 존재할 이유가 없어진다.
|
| 결제 uid 로 하나씩 적는다. 멱등 키 접두사(`ctl…`) 같은 규칙으로 빼지 않는다 —
| 나중에 진짜 결제가 우연히 같은 모양이면 그 사고도 같이 숨는다.
|
| 뺀 결제는 매번 "제외" 로 출력된다. 목록이 있다는 사실 자체가 가려지지 않게.
*/
$config['reconciliation_excluded'] = array(
	// failure-scenarios D-3 ③ 대조군. 요청이 짧아 재현되지 않았다(코인 1)
	'01a09e00baa67f84b1de97629bdab651' => 'D-3 대조군 1차 (2026-09-14, 재현 안 됨)',

	// D-3 ③ 대조군. 검사→쓰기 창을 벌려 코인 2회 지급을 일부러 재현했다
	'01a09e02e4337df0869e2da42a5ba74b' => 'D-3 대조군 2차 (2026-09-14, 코인 2회 지급을 일부러 재현)',
);
